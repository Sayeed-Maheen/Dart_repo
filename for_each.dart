void main() {
  Map info = {"name": "Sayeed", 'age': 25},
      info2 = {"name": "Hassan", 'age': 26};
  info.forEach((key, value) => print('${key}: ${value}'));
  info2.forEach((key, value) => print('${key}: ${value}'));
}
